# RM-005 Continuous Integration Gate Evidence

Date: 2026-08-21 Asia/Shanghai

Repository: `flaggielover/RouteMind` (private)

## First remote run

GitHub Actions run `32495982218` correctly failed the Java job while control and
Python/contract jobs passed. The failure log showed Linux JDK 17.0.20 was found,
but the repository script required Windows-only `javac.exe` and `mvnw.cmd` paths.

The gate was not weakened. Commit `de5e608` selected JDK tools and the Maven
Wrapper by operating system. Local Java tests and repository controls passed
before push.

## Passing remote run

GitHub Actions run `32496271644` on commit `de5e608` completed successfully:

```text
Control plane and Compose: PASS
Java business runtime: PASS
Python compute and contracts: PASS
Overall conclusion: success
```

The jobs are independent and run clean/frozen gates. An ineffective Maven cache
configuration emitted a non-failing path annotation and was removed; correctness
never depended on cached output.
