# RM-000 Stage 0 Gate Evidence

Date: 2026-08-21 Asia/Shanghai

Revision: Initial greenfield worktree before the first Git checkpoint. The exact
checkpoint is the commit containing this evidence file.

## Acceptance

- PASS: Authoritative specification, architecture, roadmap, quality gates,
  progress, handoff, decision index, and autonomous instructions exist.
- PASS: `TASK_GRAPH.yaml` contains 20 dependency-linked tasks and validates using
  only the Python standard library.
- PASS: a new session can run `scripts/resume.ps1` on a repository with or without
  prior commits and determine current and eligible work.

## Commands

```text
python scripts/validate_control_plane.py
PASS: task graph schema, dependencies, states, and evidence rules

powershell -NoProfile -ExecutionPolicy Bypass -File scripts/full-gate.ps1
PASS: required control files
PASS: PowerShell script syntax
PASS: RouteMind fast repository gate
PASS: RouteMind full available gate

powershell -NoProfile -ExecutionPolicy Bypass -File scripts/resume.ps1
PASS: recovery capsule and RouteMind fast repository gate

python -m compileall -q scripts
PASS (exit 0)

git diff --check
PASS (exit 0)
```

## Defects caught during validation

The resume smoke gate found and drove fixes for empty-history Git handling and an
escaped PowerShell task-summary expression. No known Stage 0 gate failures remain.
